#!/bin/sh
# Auto-push to GitHub after each successful commit on main.
# Enable once: git config core.hooksPath .githooks
branch=$(git symbolic-ref --short HEAD 2>/dev/null)
if [ "$branch" = "main" ]; then
  git push origin main
fi
